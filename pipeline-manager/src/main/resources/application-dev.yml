spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/pipeline
    username: pipeline
    password: ${DB_PASSWORD:pipeline123}
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    username: pipeline
    password: ${RABBITMQ_PASSWORD:pipeline123}

logging:
  level:
    com.pipeline: DEBUG

xxl:
  job:
    enabled: false
    admin:
      addresses: http://localhost:18080/xxl-job-admin
    executor:
      appname: pipeline-admin-dev
      logpath: logs/xxl-job